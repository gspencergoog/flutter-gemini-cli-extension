# Copyright 2025 The Flutter Authors.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

name: prompter
description: A command-line tool to generate .toml files from .prompt files.
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  path: ^1.9.0
  dotprompt_dart: ^0.3.0
  args: ^2.7.0
  watcher: ^1.1.4
  file: ^7.0.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
